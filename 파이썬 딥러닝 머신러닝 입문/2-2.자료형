2-1 : 숫자형
-int : 정수형
-float : 실수형
type(val1)


2-2 : 문자열
-따옴표 안에 문자를 입력하면 문자열 데이터로 인식
str1 = "EASY"
str1

-문자열 인덱스는 0부터 시작, 원소를 선택할 때는 대괄호 안에 위치 순서를 나타내는 인덱스 숫자 입력
str1[0]

-인덱스 배열의 순서를 뒤에서 앞으로 하면 뒤부터 -1로 시작하면서 앞으로 갈수록 작아진다.

-슬라이싱 : 인덱스 범위를 지정하여 여러개의 원소를 추출
str1[0:3]

-문자열 중간에 끼워 넣는 방법은 "%s" % str1


2-3 : 리스트
-여러개의 데이터를 하나의 집합으로 만듬
list = [1, 2, 3]
list

-리스트는 리스트를 원소로 가질수 있음
list1 = [list2, list3]

-리스트 인덱스도 0부터 시작

-리스트 안의 리스트는 대괄호 2개를 씀 : list[0][0]

-리스트 슬라이싱은 지정한 범위에서 범위의 끝인 2전까지 출력
list[0:2]

-리스트에 새호운 원소추가는 .append 메소드 사용
list.append(100)  : 맨끝 원소로 추가


2-4 : 투플
-리시트와 비슷한 자료구조, 투플은 ()안에 원소를 입력, 원소를 추가하거나 삭제할 수 없다
tuple = (1, 2)
tuple[0]


2-5 : 딕셔너리
-
